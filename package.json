{
	"name": "add-attachment",
	"version": "0.6.0",
	"description": "Add multiple attachment files to a note at once with auto-inserted links, optional rename and image resize.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"deploy": "node deploy.mjs",
		"build:deploy": "npm run build && npm run deploy"
	},
	"keywords": [
		"obsidian",
		"plugin",
		"attachment"
	],
	"author": "OlegG",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^16.11.6",
		"esbuild": "0.17.3",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "^5.9.3"
	}
}
